import React, { createContext } from "react";

const userContext = createContext();

export default userContext;

// This file creates a context for user data, which can be used to share user state across components.